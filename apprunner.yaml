# DEPRECATED: AWS App Runner is no longer used.
# This configuration file is kept for reference only and should not be deployed.

version: 1.0
runtime: docker
build:
  dockerfilePath: Dockerfile
run:
  port: 8080
  env:
    - name: DATABASE_URL
      value: sqlite:///delivery_platform.db
    - name: SECRET_KEY
      value: production-secret-key-2025
