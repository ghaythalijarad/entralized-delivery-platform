version: 1.0
runtime: docker
build:
  dockerfilePath: Dockerfile
run:
  port: 8080
  env:
    - name: DATABASE_URL
      value: sqlite:///delivery_platform.db
    - name: SECRET_KEY
      value: production-secret-key-2025
