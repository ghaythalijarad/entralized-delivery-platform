version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Starting pre-build phase"
    build:
      commands:
        - echo "Build phase - static files ready"
    postBuild:
      commands:
        - echo "Post-build completed"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []