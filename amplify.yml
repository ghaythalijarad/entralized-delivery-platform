version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No frontend build needed, serving static files."
    build:
      commands:
        - echo "Build completed successfully"
  artifacts:
    baseDirectory: fastapi-template/static
    files:
      - '**/*'
  cache:
    paths: []
