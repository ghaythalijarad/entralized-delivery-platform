version: 1
frontend:
  phases:
    preBuild:
      commands: []
    build:
      commands:
        - echo "Skipping build - static files"
  artifacts:
    baseDirectory: .
    files:
      - '**/*'
  cache:
    paths: []